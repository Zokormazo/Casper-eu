<div class="nav">
    <h3 class="nav-title">Menua</h3>
    <a href="#" class="nav-close">
        <span class="hidden">Itxi</span>
    </a>
    <ul>
        {{#foreach navigation}}
            <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}"><a href="{{url absolute="true"}}">{{label}}</a></li>
        {{/foreach}}
    </ul>
    {{#if @labs.subscribers}}
        <a class="subscribe-button" href="{{@blog.url}}/subscribe/">Harpidetu</a>
    {{else}}
        <a class="subscribe-button icon-feed" href="{{@blog.url}}/rss/">Harpidetu</a>
    {{/if}}
</div>
<span class="nav-cover"></span>
